"""Skills the factory has taught JARVIS (see PRD milestone-2-skill-factory.md).

Empty at rest — `teach`/`edit_skill` write modules here only after a passing
build → validate → sandbox → retrain → voice-confirm pipeline. Contents are
per-install artifacts, not source (see `.gitignore`).
"""
