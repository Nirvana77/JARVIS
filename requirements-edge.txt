# JARVIS edge (`python -m jarvis edge`) — the audio satellite.
#
#   pip install -r requirements-edge.txt
#   sudo apt install libportaudio2          # Debian/Raspberry Pi OS
#
# This list is short on purpose, and it is enforced: the edge owns a microphone
# and a speaker and runs no models at all — no Whisper, no wake word, no ONNX,
# no Piper. `tests/test_edge_imports.py` fails if an import added anywhere in
# the edge's graph drags one of those in, because the moment that happens it
# stops being installable on a Raspberry Pi.
#
# The edge still needs the repo itself (for the `jarvis` package) and a
# config.toml — see config.example.toml, [edge] section.

numpy>=2.5.3
sounddevice>=0.5.6
websockets>=17.1

# Only for a physical push-to-talk button on a Pi's GPIO ([edge] ptt_gpio):
# gpiozero>=2.0
